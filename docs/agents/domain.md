# Domain Docs

Layout: single-context.

Before exploring, engineering skills should read these when present:

- `CONTEXT.md` at the repo root
- `docs/adr/` at the repo root

If these files don't exist, proceed silently. The domain-modeling flows can create them later when terms or decisions are resolved.
